package br.com.ottech.repository;
/*
import br.com.ottech.models.Cliente;
import br.com.ottech.repositories.ClienteRepository;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.TestInstance;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
@TestInstance(TestInstance.Lifecycle.PER_CLASS)
public class ClienteRepositoryTest {

    @Autowired
    private ClienteRepository clienteRepository;

    @BeforeAll
    void start() {

        clienteRepository.deleteAll();


        clienteRepository.save(new Cliente(0L, "Fulano", "1231561"));

        clienteRepository.save(new Cliente(0L, "Ciclano", "659895654"));

        clienteRepository.save(new Cliente(0L, "Beltrano", "13465278"));
    }
}*/